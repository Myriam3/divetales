<% content_for :title, "Trips | DiveTales" %>

<section class="trips-page">
  <div class="trips-hero">
    <h1>DiveTales</h1>
    <h2>Every dive has a story.</h2>
    <p>Keep track of your dives and underwater memories.</p>
  </div>

  <div class="trips-content">
    <div class="trips-heading">
      <h2>Previous trips</h2>
      <span><%= pluralize(@trips.count, "trip") %></span>
      <%= link_to "Create a trip", new_trip_path, class: "btn-dive btn-dive-accent" %>
    </div>

    <% if @trips.any? %>
      <div class="trips-grid">
        <% @trips.each do |trip| %>
          <div class="trip-card-wrapper">
            <%= link_to trip_path(trip), class: "trip-card-link" do %>
              <article class="trip-card">
                <% if trip.cover_photo.attached? %>
                  <%= image_tag trip.cover_photo, alt: trip.title, class: "trip-card__image" %>
                <% else %>
                  <%= image_tag "trip-waves.png", alt: "Blue ocean waves", class: "trip-card__image" %>
                <% end %>

                <div class="trip-card__body">
                  <h3>
                    <%= render "shared/icon", icon_name: "luggage", icon_size: 30 %>
                    <%= trip.title %>
                  </h3>

                  <p>
                    <%= trip.start_date.strftime("%d %B %Y") %> -
                    <%= trip.end_date.strftime("%d %B %Y") %>
                  </p>

                  <% if trip.info.present? %>
                    <p><%= trip.info %></p>
                  <% end %>

                  <span class="btn-dive btn-dive-primary">View trip</span>
                </div>
              </article>
            <% end %>

            <%= form_with model: trip, url: update_cover_photo_trip_path(trip), method: :patch, html: { class: "cover-photo-form", multipart: true } do |form| %>
              <label for="trip_cover_photo_<%= trip.id %>" class="cover-photo-button">
                <%= render "shared/icon", icon_name: "camera", icon_size: 16 %>
                Edit
              </label>

              <%= form.file_field :cover_photo, id: "trip_cover_photo_#{trip.id}", accept: "image/png,image/jpeg,image/webp", class: "cover-photo-input", onchange: "this.form.requestSubmit()" %>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="trips-empty">
        <h3>No trips yet</h3>
        <p>Your saved trips will appear here.</p>
        <%= link_to "Create your first trip", new_trip_path, class: "btn-dive btn-dive-accent" %>
      </div>
    <% end %>
  </div>
</section>
