<% if dive %>
  <p>
    <%= render "shared/icon", icon_name: "calendar", label: "Date: " %>
    <%= dive.date.strftime("%d %B %Y") %>
  </p>

  <% unless local_assigns[:display_time] == false || dive.start_time.blank? %>
    <p>
      <%= render "shared/icon", icon_name: "clock", label: "Time: " %>
      <%= dive.start_time.strftime("%H:%m") %>
    </p>
  <% end %>


  <% if dive.dive_types.any? %>
    <p>
      <%= render "shared/icon", icon_name: "scuba-diving" %>
      <span>Dive types:</span>
      <% dive.dive_types.each do |type| %>
        <span class="badge text-bg-secondary">
          <%= type.capitalize %>
        </span>
      <% end %>
    </p>
  <% end %>

  <% unless local_assigns[:display_site] == false || dive.dive_site_name.blank? %>
    <p>
      <%= render "shared/icon", icon_name: "anchor", label: "Dive site: " %>
      <%= dive.dive_site_name %>
    </p>
  <% end %>

  <% unless local_assigns[:display_coordinates] == false || dive.latitude.blank? || dive.longitude.blank? %>
    <p>
      <%= render "shared/icon", icon_name: "map-pin", label: "Coordinates: " %>
      <%= dive.latitude %>,<%= dive.longitude %>
    </p>
  <% end %>


  <% unless local_assigns[:display_location] == false %>
    <p>
      <%= render "shared/icon", icon_name: "world-pin", label: "Location: " %>
      <%= dive.location.name %>
    </p>
  <% end %>

  <% unless local_assigns[:display_country] == false %>
    <p>
      <%= icon dive.location.country.code.downcase, library: "flags", width: 20, height: 20 %>
      <%= dive.location.country.name %>
    </p>
  <% end %>
<% end %>
